# ENC28J60 配置檔案清單

本文檔列出使用 ENC28J60 SPI 乙太網控制器所需的所有配置檔案及其完整內容。

---

## 配置檔案清單

### 1. 主應用程式原始碼
**檔案**: `samples/net/dhcpv4_client/src/main.c`
- 已存在，無需修改
- 負責初始化 DHCPv4 客戶端並監聽網路事件

### 2. 專案配置檔案
**檔案**: `samples/net/dhcpv4_client/prj.conf`

```conf
# ============================================
# 基本網路配置
# ============================================
CONFIG_NETWORKING=y
CONFIG_NET_IPV6=n
CONFIG_NET_IPV4=y
CONFIG_NET_ARP=y
CONFIG_NET_UDP=y

# ============================================
# DHCPv4 客戶端配置
# ============================================
CONFIG_NET_DHCPV4=y
CONFIG_NET_DHCPV4_OPTION_CALLBACKS=y

# ============================================
# DNS 配置
# ============================================
CONFIG_DNS_RESOLVER=y

# ============================================
# 隨機數生成器（用於 MAC 地址生成）
# ============================================
CONFIG_TEST_RANDOM_GENERATOR=y

# ============================================
# 堆疊監控
# ============================================
CONFIG_INIT_STACKS=y

# ============================================
# 網路管理
# ============================================
CONFIG_NET_MGMT=y
CONFIG_NET_MGMT_EVENT=y

# ============================================
# 日誌配置
# ============================================
CONFIG_NET_LOG=y
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL_DBG=y  # 可選：啟用詳細日誌

# ============================================
# 網路 Shell（用於調試，可選）
# ============================================
CONFIG_NET_SHELL=y

# ============================================
# SPI 驅動程式（如果設備樹未自動啟用）
# ============================================
CONFIG_SPI=y

# ============================================
# GPIO 驅動程式
# ============================================
CONFIG_GPIO=y

# ============================================
# ENC28J60 驅動程式配置（可選，有默認值）
# ============================================
# CONFIG_ETH_ENC28J60=y  # 自動啟用（當設備樹中有 enc28j60 節點時）
# CONFIG_ETH_ENC28J60_RX_THREAD_STACK_SIZE=800
# CONFIG_ETH_ENC28J60_RX_THREAD_PRIO=2
# CONFIG_ETH_ENC28J60_CLKRDY_INIT_WAIT_MS=2
# CONFIG_ETH_ENC28J60_TIMEOUT=100

# ============================================
# 平台特定配置（根據您的開發板調整）
# ============================================
# Nordic nRF 系列示例
CONFIG_CLOCK_CONTROL_NRF_K32SRC_RC=y

# 其他平台示例（取消註釋並修改）
# STM32 系列
# CONFIG_SOC_SERIES_STM32F4X=y
# CONFIG_CLOCK_STM32_SYSCLK_SRC_PLL=y
```

### 3. 設備樹覆蓋檔案
**檔案**: `samples/net/dhcpv4_client/overlay-enc28j60.overlay`

完整內容已在上方創建，這裡提供簡化版本供參考：

```dts
/dts-v1/;
/plugin/;

#include <dt-bindings/gpio/gpio.h>

/ {
    compatible = "your,board-compatible-string";
};

&spi0 {
    status = "okay";
    
    enc28j60: enc28j60@0 {
        compatible = "microchip,enc28j60";
        reg = <0>;
        spi-max-frequency = <10000000>;
        int-gpios = <&gpio0 5 GPIO_ACTIVE_LOW>;
        full-duplex;
        /* local-mac-address = [00 04 A3 12 34 56]; */
    };
};

&gpio0 {
    status = "okay";
};
```

### 4. CMakeLists.txt
**檔案**: `samples/net/dhcpv4_client/CMakeLists.txt`

```cmake
# SPDX-License-Identifier: Apache-2.0

cmake_minimum_required(VERSION 3.20.0)
find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})
project(dhcpv4_client)

FILE(GLOB app_sources src/*.c)
target_sources(app PRIVATE ${app_sources})
```

**狀態**: 已存在，無需修改

---

## 針對不同開發板的設備樹配置範例

### Nordic nRF52840 DK

```dts
/dts-v1/;
/plugin/;

#include <dt-bindings/gpio/gpio.h>

&spi0 {
    status = "okay";
    enc28j60: enc28j60@0 {
        compatible = "microchip,enc28j60";
        reg = <0>;
        spi-max-frequency = <10000000>;
        int-gpios = <&gpio0 5 GPIO_ACTIVE_LOW>;
        full-duplex;
    };
};

&gpio0 {
    status = "okay";
};
```

### STM32 Nucleo-F429ZI

```dts
/dts-v1/;
/plugin/;

#include <dt-bindings/gpio/gpio.h>

&spi1 {
    status = "okay";
    enc28j60: enc28j60@0 {
        compatible = "microchip,enc28j60";
        reg = <0>;
        spi-max-frequency = <10000000>;
        int-gpios = <&gpioc 6 GPIO_ACTIVE_LOW>;
        full-duplex;
    };
};

&gpioa {
    status = "okay";
};

&gpioc {
    status = "okay";
};
```

### ESP32

```dts
/dts-v1/;
/plugin/;

#include <dt-bindings/gpio/gpio.h>

&spi2 {
    status = "okay";
    enc28j60: enc28j60@0 {
        compatible = "microchip,enc28j60";
        reg = <0>;
        spi-max-frequency = <10000000>;
        int-gpios = <&gpio0 15 GPIO_ACTIVE_LOW>;
        full-duplex;
    };
};

&gpio0 {
    status = "okay";
};
```

---

## 配置檔案檢查清單

在編譯之前，請確認以下項目：

- [ ] `prj.conf` 已正確配置
  - [ ] 網路功能已啟用（CONFIG_NETWORKING=y）
  - [ ] DHCPv4 已啟用（CONFIG_NET_DHCPV4=y）
  - [ ] SPI 驅動程式已啟用（CONFIG_SPI=y）
  - [ ] GPIO 驅動程式已啟用（CONFIG_GPIO=y）

- [ ] `overlay-enc28j60.overlay` 已創建並配置
  - [ ] SPI 控制器編號正確（如 &spi0, &spi1）
  - [ ] CS 片選線編號正確（reg = <0> 或其他值）
  - [ ] SPI 頻率 ≤ 20MHz
  - [ ] 中斷 GPIO 配置正確（GPIO_ACTIVE_LOW）
  - [ ] GPIO 控制器已啟用（status = "okay"）

- [ ] `CMakeLists.txt` 已存在且正確

- [ ] 硬體連接已確認
  - [ ] SPI 線路（SCK, MOSI, MISO, CS）連接正確
  - [ ] 中斷引腳（INT）連接正確
  - [ ] 電源（3.3V）和地線（GND）連接正確
  - [ ] 網路線連接正常

---

## 編譯命令參考

### 使用 West 工具

```bash
# 基本編譯（使用預設配置）
west build -b <board_name>

# 使用設備樹覆蓋檔案編譯
west build -b <board_name> -- -DOVERLAY_CONFIG=overlay-enc28j60.overlay

# 編譯並燒錄
west build -b <board_name> -- -DOVERLAY_CONFIG=overlay-enc28j60.overlay && west flash
```

### 使用 CMake 直接編譯

```bash
# 創建構建目錄
mkdir build && cd build

# 配置專案
cmake -GNinja -DBOARD=<board_name> -DOVERLAY_CONFIG=../overlay-enc28j60.overlay ..

# 編譯
ninja

# 燒錄（根據您的燒錄工具調整）
ninja flash
```

### 常見開發板名稱範例

- Nordic nRF52840 DK: `nrf52840dk_nrf52840`
- STM32 Nucleo-F429ZI: `nucleo_f429zi`
- ESP32: `esp32`

---

## 驗證步驟

編譯完成後，按以下步驟驗證：

1. **檢查編譯輸出**
   - 確認沒有錯誤訊息
   - 確認 ENC28J60 驅動程式已被包含

2. **燒錄韌體**
   - 使用 `west flash` 或對應的燒錄工具

3. **查看串口輸出**
   - 連接串口工具（115200 波特率）
   - 查看初始化訊息
   - 確認 DHCP IP 地址已獲得

4. **網路測試**
   - Ping 開發板 IP 地址
   - 使用網路 Shell 查看狀態（如已啟用）

---

## 故障排除檢查點

如果遇到問題，請檢查：

1. **設備樹語法錯誤**
   - 使用 `west build --pristine` 清理構建
   - 檢查設備樹檔案語法

2. **SPI 配置錯誤**
   - 確認 SPI 控制器編號正確
   - 確認 CS 片選線配置正確

3. **GPIO 配置錯誤**
   - 確認中斷 GPIO 引腳編號正確
   - 確認 GPIO 控制器已啟用

4. **硬體連接問題**
   - 使用示波器檢查 SPI 信號
   - 檢查中斷信號是否正常觸發

---

## Kconfig 配置選項詳解

本節詳細說明所有與 ENC28J60 SPI 乙太網控制器相關的 Kconfig 配置選項。

### 一、ENC28J60 驅動程式配置選項

**檔案位置**: `drivers/ethernet/Kconfig.enc28j60`

#### 1. ETH_ENC28J60 (主要選項)

```kconfig
menuconfig ETH_ENC28J60
    bool "ENC28J60C Ethernet Controller"
    default y
    depends on DT_HAS_MICROCHIP_ENC28J60_ENABLED
    select SPI
```

**說明**:
- **類型**: 主選單配置（menuconfig）
- **默認值**: `y`（自動啟用）
- **依賴**: 當設備樹中存在 `compatible = "microchip,enc28j60"` 的節點時自動啟用
- **自動選擇**: 自動選擇 `SPI` 驅動程式
- **功能**: 啟用 ENC28J60 乙太網控制器驅動程式

**在 prj.conf 中的配置**:
```conf
# 通常不需要手動配置，因為當設備樹中有 enc28j60 節點時會自動啟用
# CONFIG_ETH_ENC28J60=y  # 可選：顯式啟用以確保啟用
```

#### 2. ETH_ENC28J60_RX_THREAD_STACK_SIZE

```kconfig
config ETH_ENC28J60_RX_THREAD_STACK_SIZE
    int "Stack size for internal incoming packet handler"
    depends on ETH_ENC28J60
    default 800
```

**說明**:
- **類型**: 整數配置
- **默認值**: `800`（字節）
- **依賴**: `ETH_ENC28J60`
- **功能**: 設置接收執行緒的堆疊大小
- **建議值**: 
  - 最小: 512（可能不夠）
  - 默認: 800（推薦）
  - 大型應用: 1024-2048

**在 prj.conf 中的配置**:
```conf
# 如果需要更大的堆疊（例如處理大型封包或複雜應用）
CONFIG_ETH_ENC28J60_RX_THREAD_STACK_SIZE=1024
```

#### 3. ETH_ENC28J60_RX_THREAD_PRIO

```kconfig
config ETH_ENC28J60_RX_THREAD_PRIO
    int "Priority for internal incoming packet handler"
    depends on ETH_ENC28J60
    default 2
```

**說明**:
- **類型**: 整數配置
- **默認值**: `2`
- **依賴**: `ETH_ENC28J60`
- **功能**: 設置接收執行緒的優先級
- **優先級說明**:
  - Zephyr 協同多工優先級範圍通常為 0-14（數字越小優先級越高）
  - `0`: 最高優先級
  - `2`: 默認優先級（推薦用於網路接收）
  - `5-7`: 中等優先級
  - `14`: 最低優先級
- **建議值**: 
  - 高性能網路應用: `1-2`
  - 一般應用: `2-3`（默認）
  - 低優先級應用: `4-5`

**在 prj.conf 中的配置**:
```conf
# 如果需要更高的接收優先級
CONFIG_ETH_ENC28J60_RX_THREAD_PRIO=1

# 或降低優先級（如果網路不是關鍵任務）
CONFIG_ETH_ENC28J60_RX_THREAD_PRIO=5
```

#### 4. ETH_ENC28J60_CLKRDY_INIT_WAIT_MS

```kconfig
config ETH_ENC28J60_CLKRDY_INIT_WAIT_MS
    int "Time to wait for the CLKRDY bit on driver init"
    depends on ETH_ENC28J60
    default 2
```

**說明**:
- **類型**: 整數配置（毫秒）
- **默認值**: `2`（毫秒）
- **依賴**: `ETH_ENC28J60`
- **功能**: 驅動程式初始化時等待 CLKRDY（時鐘就緒）位元的最大時間
- **技術背景**: ENC28J60 需要等待內部振盪器穩定時鐘（OST - Oscillator Start-up Timer）後才能工作
- **建議值**:
  - 一般情況: `2`（默認，推薦）
  - 時鐘不穩定或延遲啟動: `5-10`
  - 超時處理: 如果超過此時間，驅動程式初始化將失敗並返回 `-ETIMEDOUT`

**在 prj.conf 中的配置**:
```conf
# 如果遇到初始化超時問題，可以增加等待時間
CONFIG_ETH_ENC28J60_CLKRDY_INIT_WAIT_MS=5
```

#### 5. ETH_ENC28J60_TIMEOUT

```kconfig
config ETH_ENC28J60_TIMEOUT
    int "IP buffer timeout"
    depends on ETH_ENC28J60
    default 100
```

**說明**:
- **類型**: 整數配置（毫秒）
- **默認值**: `100`（毫秒）
- **依賴**: `ETH_ENC28J60`
- **功能**: IP 堆疊分配緩衝區的最大等待時間
- **技術背景**: 當驅動程式需要從 IP 堆疊獲取網路緩衝區來存儲接收的乙太網幀時，如果在此時間內無法獲得緩衝區，則丟棄該幀
- **建議值**:
  - 一般應用: `100`（默認）
  - 低記憶體系統: `50-100`
  - 高流量應用: `100-200`

**在 prj.conf 中的配置**:
```conf
# 如果遇到封包丟失問題，可以增加超時時間
CONFIG_ETH_ENC28J60_TIMEOUT=200
```

#### 6. ETH_INIT_PRIORITY

```kconfig
config ETH_INIT_PRIORITY
    int "Ethernet driver init priority"
    default 80
    depends on NET_L2_ETHERNET || ETH_DRIVER
```

**說明**:
- **類型**: 整數配置
- **默認值**: `80`
- **依賴**: `NET_L2_ETHERNET` 或 `ETH_DRIVER`
- **功能**: 設定乙太網驅動程式的初始化優先級
- **注意**: 此優先級必須低於網路堆疊的初始化優先級，以確保驅動程式在網路堆疊之前啟動
- **建議值**: 通常不需要修改，保持默認值 `80`

**在 prj.conf 中的配置**:
```conf
# 通常不需要修改，除非有特殊需求
# CONFIG_ETH_INIT_PRIORITY=80
```

### 二、網路堆疊配置選項

#### 1. NETWORKING

```kconfig
config NETWORKING
    bool "Link layer and networking (including IP)"
    select NET_BUF
    select POLL
    select ENTROPY_GENERATOR
```

**說明**:
- **類型**: 布林配置
- **功能**: 啟用連結層和網路支援（包括 IP）
- **必需**: 是（必須啟用）

**在 prj.conf 中的配置**:
```conf
CONFIG_NETWORKING=y
```

#### 2. NET_IPV4

**說明**:
- **功能**: 啟用 IPv4 協議支援
- **必需**: 是（DHCPv4 需要 IPv4）

**在 prj.conf 中的配置**:
```conf
CONFIG_NET_IPV4=y
CONFIG_NET_IPV6=n  # 禁用 IPv6（如果不需要）
```

#### 3. NET_ARP

```kconfig
config NET_ARP
    bool "ARP"
    default y
    depends on NET_IPV4
```

**說明**:
- **類型**: 布林配置
- **默認值**: `y`
- **依賴**: `NET_IPV4`
- **功能**: 啟用 ARP（地址解析協議）支援
- **必需**: 是（乙太網需要 ARP）

**在 prj.conf 中的配置**:
```conf
CONFIG_NET_ARP=y
```

#### 4. NET_UDP

**說明**:
- **功能**: 啟用 UDP 協議支援
- **必需**: 是（DHCPv4 使用 UDP）

**在 prj.conf 中的配置**:
```conf
CONFIG_NET_UDP=y
```

#### 5. NET_L2_ETHERNET

```kconfig
menuconfig NET_L2_ETHERNET
    bool "Ethernet support"
```

**說明**:
- **類型**: 主選單配置
- **功能**: 啟用乙太網 L2 層支援
- **必需**: 是（ENC28J60 是乙太網設備）

**在 prj.conf 中的配置**:
```conf
# 通常會自動啟用，當 ETH_ENC28J60 啟用時
# CONFIG_NET_L2_ETHERNET=y
```

#### 6. NET_MGMT 和 NET_MGMT_EVENT

**說明**:
- **NET_MGMT**: 啟用網路管理介面
- **NET_MGMT_EVENT**: 啟用網路管理事件
- **必需**: 是（DHCPv4 需要）

**在 prj.conf 中的配置**:
```conf
CONFIG_NET_MGMT=y
CONFIG_NET_MGMT_EVENT=y
```

### 三、DHCPv4 配置選項

**檔案位置**: `subsys/net/lib/dhcpv4/Kconfig`

#### 1. NET_DHCPV4

```kconfig
config NET_DHCPV4
    bool "DHCPv4 client"
    select NET_MGMT
    select NET_MGMT_EVENT
    select NET_MGMT_EVENT_INFO if NET_IPV4_ACD
    depends on NET_IPV4 && NET_UDP
```

**說明**:
- **類型**: 布林配置
- **功能**: 啟用 DHCPv4 客戶端
- **依賴**: `NET_IPV4` 和 `NET_UDP`
- **自動選擇**: `NET_MGMT` 和 `NET_MGMT_EVENT`

**在 prj.conf 中的配置**:
```conf
CONFIG_NET_DHCPV4=y
```

#### 2. NET_DHCPV4_OPTION_CALLBACKS

```kconfig
config NET_DHCPV4_OPTION_CALLBACKS
    bool "Option callbacks"
```

**說明**:
- **類型**: 布林配置
- **功能**: 啟用自定義 DHCP 選項回調
- **用途**: 允許應用程式處理 DHCP 伺服器發送的自定義選項（如 NTP 伺服器）

**在 prj.conf 中的配置**:
```conf
CONFIG_NET_DHCPV4_OPTION_CALLBACKS=y
```

#### 3. NET_DHCPV4_INITIAL_DELAY_MAX

```kconfig
config NET_DHCPV4_INITIAL_DELAY_MAX
    int "Maximum time out for initial discover request"
    default 10
    range 2 10
```

**說明**:
- **類型**: 整數配置（秒）
- **默認值**: `10`
- **範圍**: `2-10`
- **功能**: 初始 DHCP Discover 請求的最大延遲時間
- **技術背景**: 根據 RFC2131 4.1.1，在發送初始 Discover 之前等待 1-10 秒的隨機時間

**在 prj.conf 中的配置**:
```conf
# 通常使用默認值即可
# CONFIG_NET_DHCPV4_INITIAL_DELAY_MAX=10
```

#### 4. NET_DHCPV4_ACCEPT_UNICAST

```kconfig
config NET_DHCPV4_ACCEPT_UNICAST
    bool "Accept unicast DHCPv4 traffic"
    default y
```

**說明**:
- **類型**: 布林配置
- **默認值**: `y`
- **功能**: 允許接收單播 DHCPv4 回應（在配置 IP 地址之前）

**在 prj.conf 中的配置**:
```conf
# 通常使用默認值即可
# CONFIG_NET_DHCPV4_ACCEPT_UNICAST=y
```

#### 5. NET_DHCPV4_OPTION_DNS_ADDRESS

```kconfig
config NET_DHCPV4_OPTION_DNS_ADDRESS
    bool "Use DNS server from DHCPv4 option and save it in the DNS resolver default context"
    default y
    depends on DNS_RESOLVER
```

**說明**:
- **類型**: 布林配置
- **默認值**: `y`
- **依賴**: `DNS_RESOLVER`
- **功能**: 從 DHCPv4 選項中獲取 DNS 伺服器地址並保存到 DNS 解析器

**在 prj.conf 中的配置**:
```conf
CONFIG_DNS_RESOLVER=y
CONFIG_NET_DHCPV4_OPTION_DNS_ADDRESS=y
```

### 四、SPI 驅動程式配置選項

**檔案位置**: `drivers/spi/Kconfig`

#### 1. SPI

```kconfig
menuconfig SPI
    bool "Serial Peripheral Interface (SPI) bus drivers"
```

**說明**:
- **類型**: 主選單配置
- **功能**: 啟用 SPI 匯流排驅動程式
- **自動啟用**: 當 `ETH_ENC28J60` 啟用時會自動選擇

**在 prj.conf 中的配置**:
```conf
# 通常不需要手動配置，因為 ETH_ENC28J60 會自動選擇
# CONFIG_SPI=y
```

#### 2. SPI_INIT_PRIORITY

```kconfig
config SPI_INIT_PRIORITY
    int "Init priority"
    default KERNEL_INIT_PRIORITY_DEVICE
```

**說明**:
- **類型**: 整數配置
- **默認值**: `KERNEL_INIT_PRIORITY_DEVICE`（通常為 50）
- **功能**: SPI 驅動程式初始化優先級
- **建議**: 通常不需要修改

#### 3. SPI_COMPLETION_TIMEOUT_TOLERANCE

```kconfig
config SPI_COMPLETION_TIMEOUT_TOLERANCE
    int "Completion timeout tolerance (ms)"
    default 200
```

**說明**:
- **類型**: 整數配置（毫秒）
- **默認值**: `200`
- **功能**: SPI 完成超時的容差值
- **建議**: 通常不需要修改

### 五、GPIO 驅動程式配置選項

**檔案位置**: `drivers/gpio/Kconfig`

#### 1. GPIO

```kconfig
menuconfig GPIO
    bool "General-Purpose Input/Output (GPIO) drivers"
```

**說明**:
- **類型**: 主選單配置
- **功能**: 啟用 GPIO 驅動程式
- **必需**: 是（ENC28J60 的中斷需要 GPIO）

**在 prj.conf 中的配置**:
```conf
CONFIG_GPIO=y
```

#### 2. GPIO_INIT_PRIORITY

```kconfig
config GPIO_INIT_PRIORITY
    int "GPIO init priority"
    default KERNEL_INIT_PRIORITY_DEFAULT
```

**說明**:
- **類型**: 整數配置
- **默認值**: `KERNEL_INIT_PRIORITY_DEFAULT`（通常為 40）
- **功能**: GPIO 驅動程式初始化優先級
- **建議**: 通常不需要修改

### 六、日誌配置選項

#### 1. ETHERNET_LOG_LEVEL

**說明**:
- **功能**: 設定乙太網驅動程式的日誌級別
- **級別**: `OFF` (0), `ERR` (1), `WRN` (2), `INF` (3), `DBG` (4)

**在 prj.conf 中的配置**:
```conf
# 啟用詳細日誌（用於調試）
CONFIG_ETHERNET_LOG_LEVEL_DBG=y

# 或只啟用資訊日誌
CONFIG_ETHERNET_LOG_LEVEL_INF=y
```

#### 2. NET_LOG

**說明**:
- **功能**: 啟用網路堆疊日誌

**在 prj.conf 中的配置**:
```conf
CONFIG_NET_LOG=y
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL_DBG=y  # 詳細日誌級別
```

#### 3. NET_DHCPV4_LOG_LEVEL

**說明**:
- **功能**: 設定 DHCPv4 客戶端的日誌級別

**在 prj.conf 中的配置**:
```conf
# 啟用 DHCPv4 詳細日誌
CONFIG_NET_DHCPV4_LOG_LEVEL_DBG=y
```

### 七、完整的 Kconfig 配置範例

以下是 `prj.conf` 中完整的 Kconfig 配置範例，包含所有必要的選項和說明：

```conf
# ============================================
# 基本網路配置
# ============================================
CONFIG_NETWORKING=y
CONFIG_NET_IPV6=n
CONFIG_NET_IPV4=y
CONFIG_NET_ARP=y
CONFIG_NET_UDP=y

# ============================================
# DHCPv4 客戶端配置
# ============================================
CONFIG_NET_DHCPV4=y
CONFIG_NET_DHCPV4_OPTION_CALLBACKS=y
CONFIG_NET_DHCPV4_INITIAL_DELAY_MAX=10
CONFIG_NET_DHCPV4_ACCEPT_UNICAST=y

# ============================================
# DNS 配置
# ============================================
CONFIG_DNS_RESOLVER=y
CONFIG_NET_DHCPV4_OPTION_DNS_ADDRESS=y

# ============================================
# 網路管理
# ============================================
CONFIG_NET_MGMT=y
CONFIG_NET_MGMT_EVENT=y

# ============================================
# ENC28J60 驅動程式配置（自動啟用，但可自定義）
# ============================================
CONFIG_ETH_ENC28J60=y                    # 自動啟用（如果設備樹中有節點）
CONFIG_ETH_ENC28J60_RX_THREAD_STACK_SIZE=800
CONFIG_ETH_ENC28J60_RX_THREAD_PRIO=2
CONFIG_ETH_ENC28J60_CLKRDY_INIT_WAIT_MS=2
CONFIG_ETH_ENC28J60_TIMEOUT=100
CONFIG_ETH_INIT_PRIORITY=80

# ============================================
# SPI 驅動程式（自動啟用）
# ============================================
CONFIG_SPI=y
# CONFIG_SPI_INIT_PRIORITY=50  # 通常不需要修改

# ============================================
# GPIO 驅動程式
# ============================================
CONFIG_GPIO=y
# CONFIG_GPIO_INIT_PRIORITY=40  # 通常不需要修改

# ============================================
# 日誌配置
# ============================================
CONFIG_LOG=y
CONFIG_NET_LOG=y
CONFIG_LOG_DEFAULT_LEVEL_DBG=y
CONFIG_ETHERNET_LOG_LEVEL_DBG=y
CONFIG_NET_DHCPV4_LOG_LEVEL_DBG=y

# ============================================
# 網路 Shell（可選，用於調試）
# ============================================
CONFIG_NET_SHELL=y

# ============================================
# 其他必要配置
# ============================================
CONFIG_TEST_RANDOM_GENERATOR=y
CONFIG_INIT_STACKS=y
```

### 八、Kconfig 配置檢查清單

在編譯之前，請確認以下 Kconfig 選項已正確配置：

- [ ] **基本網路配置**
  - [ ] `CONFIG_NETWORKING=y`
  - [ ] `CONFIG_NET_IPV4=y`
  - [ ] `CONFIG_NET_ARP=y`
  - [ ] `CONFIG_NET_UDP=y`

- [ ] **DHCPv4 配置**
  - [ ] `CONFIG_NET_DHCPV4=y`
  - [ ] `CONFIG_NET_DHCPV4_OPTION_CALLBACKS=y`（如使用選項回調）
  - [ ] `CONFIG_NET_MGMT=y`
  - [ ] `CONFIG_NET_MGMT_EVENT=y`

- [ ] **ENC28J60 驅動程式**
  - [ ] `CONFIG_ETH_ENC28J60=y`（自動啟用）
  - [ ] `CONFIG_ETH_ENC28J60_RX_THREAD_STACK_SIZE`（可選，自定義）
  - [ ] `CONFIG_ETH_ENC28J60_RX_THREAD_PRIO`（可選，自定義）
  - [ ] `CONFIG_ETH_ENC28J60_CLKRDY_INIT_WAIT_MS`（可選，自定義）
  - [ ] `CONFIG_ETH_ENC28J60_TIMEOUT`（可選，自定義）

- [ ] **SPI 驅動程式**
  - [ ] `CONFIG_SPI=y`（自動啟用）

- [ ] **GPIO 驅動程式**
  - [ ] `CONFIG_GPIO=y`

- [ ] **日誌配置**
  - [ ] `CONFIG_LOG=y`
  - [ ] `CONFIG_NET_LOG=y`（可選）

### 九、常見 Kconfig 配置問題

#### 問題 1: 驅動程式未啟用

**症狀**: 編譯錯誤，提示找不到 ENC28J60 驅動程式

**解決方法**:
- 確認設備樹中有 `compatible = "microchip,enc28j60"` 節點
- 或手動在 `prj.conf` 中添加 `CONFIG_ETH_ENC28J60=y`

#### 問題 2: 接收執行緒堆疊溢出

**症狀**: 系統崩潰或異常行為

**解決方法**:
- 增加 `CONFIG_ETH_ENC28J60_RX_THREAD_STACK_SIZE` 的值
- 例如：從 800 增加到 1024 或 2048

#### 問題 3: 初始化超時

**症狀**: 驅動程式初始化失敗，返回 `-ETIMEDOUT`

**解決方法**:
- 增加 `CONFIG_ETH_ENC28J60_CLKRDY_INIT_WAIT_MS` 的值
- 例如：從 2 增加到 5 或 10

#### 問題 4: 封包丟失

**症狀**: 接收到的封包被丟棄

**解決方法**:
- 增加 `CONFIG_ETH_ENC28J60_TIMEOUT` 的值
- 增加 `CONFIG_ETH_ENC28J60_RX_THREAD_STACK_SIZE`
- 檢查系統記憶體是否充足

#### 問題 5: DHCP 客戶端無法啟動

**症狀**: 無法獲取 IP 地址

**解決方法**:
- 確認 `CONFIG_NET_DHCPV4=y`
- 確認 `CONFIG_NET_MGMT=y` 和 `CONFIG_NET_MGMT_EVENT=y`
- 確認 `CONFIG_NET_IPV4=y` 和 `CONFIG_NET_UDP=y`

## DHCPv4 Client 專案代碼樹狀結構

本節詳細說明整個 `dhcpv4_client` 專案的代碼樹狀結構，包括所有相關目錄和文件。

### 一、專案根目錄結構

```
zephyr/
├── samples/net/dhcpv4_client/           # DHCPv4 Client 範例專案根目錄
│   ├── src/                              # 應用程式原始碼目錄
│   │   └── main.c                        # 主應用程式原始碼
│   ├── boards/                           # 特定開發板的配置檔案
│   │   ├── fvp_base_revc_2xaemv8a.conf  # Arm FVP Base RevC 配置
│   │   ├── fvp_base_revc_2xaemv8a.overlay
│   │   ├── fvp_baser_aemv8r.conf        # Arm FVP Baser 配置
│   │   ├── fvp_baser_aemv8r.overlay
│   │   └── nrf54l15dk_nrf54l15_cpuapp.conf
│   ├── CMakeLists.txt                    # CMake 構建檔案
│   ├── prj.conf                          # 專案配置檔案（Kconfig）
│   ├── overlay-enc28j60.overlay         # ENC28J60 設備樹覆蓋檔案
│   ├── overlay-dm9051.overlay           # DM9051 設備樹覆蓋檔案（其他範例）
│   ├── overlay-e1000.conf               # E1000 配置範例
│   ├── README.rst                        # 專案說明文件
│   └── sample.yaml                       # 範例元數據檔案
│
└── drivers/ethernet/                     # 乙太網驅動程式目錄
    ├── CMakeLists.txt                    # 乙太網驅動程式 CMake 構建檔案
    ├── eth_enc28j60.c                    # ENC28J60 驅動程式實作
    ├── eth_enc28j60_priv.h               # ENC28J60 私有標頭檔案
    ├── Kconfig                           # 乙太網驅動程式 Kconfig
    ├── Kconfig.enc28j60                  # ENC28J60 專用 Kconfig
    ├── eth.h                             # 乙太網通用標頭檔案
    ├── phy/                              # PHY 驅動程式子目錄
    │   ├── CMakeLists.txt
    │   ├── Kconfig
    │   ├── phy_mii.c
    │   ├── phy_adin2111.c
    │   ├── phy_dm8806.c
    │   └── ...                           # 其他 PHY 驅動程式
    ├── dsa/                              # DSA 驅動程式子目錄
    │   ├── CMakeLists.txt
    │   ├── Kconfig
    │   └── dsa_ksz8xxx.c
    ├── eth_nxp_enet_qos/                 # NXP ENET QoS 子目錄
    │   ├── CMakeLists.txt
    │   ├── Kconfig
    │   ├── eth_nxp_enet_qos.c
    │   └── eth_nxp_enet_qos_mac.c
    ├── nxp_imx_netc/                     # NXP i.MX NETC 子目錄
    │   ├── CMakeLists.txt
    │   └── ...
    └── dwc_xgmac/                        # DWC XGMAC 子目錄
        ├── CMakeLists.txt
        └── ...
```

### 二、DHCPv4 Client 專案文件詳解

#### 1. `samples/net/dhcpv4_client/CMakeLists.txt`

**檔案內容**:
```cmake
# SPDX-License-Identifier: Apache-2.0

cmake_minimum_required(VERSION 3.20.0)
find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})
project(dhcpv4_client)

FILE(GLOB app_sources src/*.c)
target_sources(app PRIVATE ${app_sources})
```

**說明**:
- **`cmake_minimum_required(VERSION 3.20.0)`**: 指定 CMake 最低版本要求（3.20.0）
- **`find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})`**: 查找並載入 Zephyr RTOS 套件
  - `REQUIRED`: 如果找不到 Zephyr 套件，CMake 會報錯
  - `HINTS $ENV{ZEPHYR_BASE}`: 從環境變數 `ZEPHYR_BASE` 中尋找 Zephyr 基本路徑
- **`project(dhcpv4_client)`**: 定義專案名稱
- **`FILE(GLOB app_sources src/*.c)`**: 使用 GLOB 模式匹配所有 `.c` 檔案並儲存到變數 `app_sources`
  - 匹配 `src/` 目錄下的所有 `.c` 檔案
- **`target_sources(app PRIVATE ${app_sources})`**: 將源檔案添加到 `app` 目標
  - `PRIVATE`: 這些源檔案僅在此目標中使用，不會傳遞給依賴此目標的其他目標

**功能**: 此 CMakeLists.txt 負責：
1. 初始化 CMake 並載入 Zephyr 構建系統
2. 定義專案名稱
3. 自動收集 `src/` 目錄下的所有 C 源檔案
4. 將源檔案添加到應用程式構建目標

#### 2. `drivers/ethernet/CMakeLists.txt`

**檔案內容**:
```cmake
# SPDX-License-Identifier: Apache-2.0

zephyr_library()
zephyr_library_property(ALLOW_EMPTY TRUE)
zephyr_library_include_directories(${ZEPHYR_BASE}/subsys/net/l2)

# Raw mode driver
zephyr_library_sources_ifdef(CONFIG_ETH_DRIVER_RAW_MODE
  eth_raw.c
  )

# Gecko Ethernet driver
zephyr_library_sources_ifdef(CONFIG_ETH_GECKO
	eth_gecko.c
	phy_gecko.c
	)

# Xilinx GEM Ethernet driver
zephyr_library_sources_ifdef(CONFIG_ETH_XLNX_GEM
	eth_xlnx_gem.c
	phy_xlnx_gem.c
	)

# DWMAC drivers
zephyr_library_sources_ifdef(CONFIG_ETH_DWMAC		eth_dwmac.c)
zephyr_library_sources_ifdef(CONFIG_ETH_DWMAC_STM32H7X	eth_dwmac_stm32h7x.c)
zephyr_library_sources_ifdef(CONFIG_ETH_DWMAC_MMU	eth_dwmac_mmu.c)

# Individual Ethernet driver implementations
zephyr_library_sources_ifdef(CONFIG_ETH_E1000		eth_e1000.c)
zephyr_library_sources_ifdef(CONFIG_ETH_ENC28J60	eth_enc28j60.c)
zephyr_library_sources_ifdef(CONFIG_ETH_DM9051		eth_dm9051.c)
zephyr_library_sources_ifdef(CONFIG_ETH_ENC424J600	eth_enc424j600.c)
zephyr_library_sources_ifdef(CONFIG_ETH_ESP32		eth_esp32.c)
zephyr_library_sources_ifdef(CONFIG_ETH_LITEX_LITEETH	eth_litex_liteeth.c)
zephyr_library_sources_ifdef(CONFIG_ETH_SMSC911X	eth_smsc911x.c)
zephyr_library_sources_ifdef(CONFIG_ETH_STELLARIS	eth_stellaris.c)
zephyr_library_sources_ifdef(CONFIG_ETH_STM32_HAL	eth_stm32_hal.c)
zephyr_library_sources_ifdef(CONFIG_ETH_W5500		eth_w5500.c)
zephyr_library_sources_ifdef(CONFIG_ETH_SAM_GMAC	eth_sam_gmac.c)
zephyr_library_sources_ifdef(CONFIG_ETH_CYCLONEV	eth_cyclonev.c)
zephyr_library_sources_ifdef(CONFIG_SLIP_TAP		eth_slip_tap.c)
zephyr_library_sources_ifdef(CONFIG_ETH_SMSC91X		eth_smsc91x.c)
zephyr_library_sources_ifdef(CONFIG_ETH_IVSHMEM 	eth_ivshmem.c eth_ivshmem_queue.c)
zephyr_library_sources_ifdef(CONFIG_ETH_ADIN2111	eth_adin2111.c)
zephyr_library_sources_ifdef(CONFIG_ETH_LAN865X	        eth_lan865x.c oa_tc6.c)
zephyr_library_sources_ifdef(CONFIG_ETH_XMC4XXX		eth_xmc4xxx.c)
zephyr_library_sources_ifdef(CONFIG_ETH_TEST		eth_test.c)
zephyr_library_sources_ifdef(CONFIG_ETH_RENESAS_RA	eth_renesas_ra.c)
zephyr_library_sources_ifdef(CONFIG_ETH_LAN9250		eth_lan9250.c)
zephyr_library_sources_ifdef(CONFIG_ETH_SY1XX		eth_sensry_sy1xx_mac.c)
zephyr_library_sources_ifdef(CONFIG_ETH_NXP_ENET	eth_nxp_enet.c)
zephyr_library_sources_ifdef(CONFIG_ETH_XILINX_AXIENET	eth_xilinx_axienet.c)

# NXP S32 NETC driver (conditional compilation)
if(CONFIG_ETH_NXP_S32_NETC)
  zephyr_library_sources(eth_nxp_s32_netc.c)
  zephyr_library_sources_ifdef(CONFIG_DT_HAS_NXP_S32_NETC_PSI_ENABLED	eth_nxp_s32_netc_psi.c)
  zephyr_library_sources_ifdef(CONFIG_DT_HAS_NXP_S32_NETC_VSI_ENABLED	eth_nxp_s32_netc_vsi.c)
endif()

zephyr_library_sources_ifdef(CONFIG_ETH_NXP_S32_GMAC	eth_nxp_s32_gmac.c)
zephyr_library_sources_ifdef(CONFIG_ETH_NUMAKER		eth_numaker.c)

# Native TAP driver (platform-specific)
if(CONFIG_ETH_NATIVE_TAP)
  if (CONFIG_NATIVE_APPLICATION)
    set(native_tap_source_files eth_native_tap.c eth_native_tap_adapt.c)
    set_source_files_properties(${native_tap_source_files}
      PROPERTIES COMPILE_DEFINITIONS
      "NO_POSIX_CHEATS;_BSD_SOURCE;_DEFAULT_SOURCE"
    )
    zephyr_library_sources(${native_tap_source_files})
  else()
    zephyr_library_sources(eth_native_tap.c)
    target_sources(native_simulator INTERFACE eth_native_tap_adapt.c)
  endif()
endif()

# Add subdirectories for specialized drivers
add_subdirectory(dsa)
add_subdirectory(phy)
add_subdirectory(eth_nxp_enet_qos)
add_subdirectory(nxp_imx_netc)
add_subdirectory(dwc_xgmac)
```

**詳細說明**:

##### 基本設定

```cmake
zephyr_library()
```
- **功能**: 創建一個 Zephyr 庫目標
- **說明**: 這會創建一個名為 `drivers__ethernet` 的靜態庫目標，用於包含所有乙太網驅動程式

```cmake
zephyr_library_property(ALLOW_EMPTY TRUE)
```
- **功能**: 允許庫為空
- **說明**: 如果沒有啟用任何驅動程式，不會報錯。這允許條件編譯

```cmake
zephyr_library_include_directories(${ZEPHYR_BASE}/subsys/net/l2)
```
- **功能**: 添加包含目錄
- **說明**: 將 `subsys/net/l2` 目錄添加到編譯器的包含路徑，以便驅動程式可以訪問 L2 層的標頭檔案

##### 條件編譯驅動程式

```cmake
zephyr_library_sources_ifdef(CONFIG_ETH_ENC28J60	eth_enc28j60.c)
```
- **功能**: 條件編譯 ENC28J60 驅動程式
- **說明**: 
  - `CONFIG_ETH_ENC28J60`: Kconfig 配置選項名稱
  - 僅當此選項為 `y` 時，才會編譯 `eth_enc28j60.c`
  - 當設備樹中有 `compatible = "microchip,enc28j60"` 節點時，`CONFIG_ETH_ENC28J60` 會自動設置為 `y`

**ENC28J60 驅動程式相關**:
- **檔案**: `eth_enc28j60.c` - 驅動程式實作
- **配置**: `CONFIG_ETH_ENC28J60` - 由 Kconfig 控制
- **依賴**: 
  - SPI 驅動程式（自動選擇）
  - GPIO 驅動程式（用於中斷）

##### 其他驅動程式範例

**多檔案驅動程式**:
```cmake
zephyr_library_sources_ifdef(CONFIG_ETH_GECKO
	eth_gecko.c
	phy_gecko.c
	)
```
- Gecko 驅動程式需要兩個源檔案：主驅動程式和 PHY 驅動程式

**條件邏輯驅動程式**:
```cmake
if(CONFIG_ETH_NXP_S32_NETC)
  zephyr_library_sources(eth_nxp_s32_netc.c)
  zephyr_library_sources_ifdef(CONFIG_DT_HAS_NXP_S32_NETC_PSI_ENABLED	eth_nxp_s32_netc_psi.c)
  zephyr_library_sources_ifdef(CONFIG_DT_HAS_NXP_S32_NETC_VSI_ENABLED	eth_nxp_s32_netc_vsi.c)
endif()
```
- 使用 `if()` 進行複雜的條件編譯
- 主檔案始終編譯，子功能根據設備樹屬性條件編譯

**平台特定驅動程式**:
```cmake
if(CONFIG_ETH_NATIVE_TAP)
  if (CONFIG_NATIVE_APPLICATION)
    set(native_tap_source_files eth_native_tap.c eth_native_tap_adapt.c)
    set_source_files_properties(${native_tap_source_files}
      PROPERTIES COMPILE_DEFINITIONS
      "NO_POSIX_CHEATS;_BSD_SOURCE;_DEFAULT_SOURCE"
    )
    zephyr_library_sources(${native_tap_source_files})
  else()
    zephyr_library_sources(eth_native_tap.c)
    target_sources(native_simulator INTERFACE eth_native_tap_adapt.c)
  endif()
endif()
```
- 根據應用程式類型（原生應用或模擬器）使用不同的源檔案
- 使用 `set_source_files_properties()` 設置編譯定義

##### 子目錄

```cmake
add_subdirectory(dsa)
add_subdirectory(phy)
add_subdirectory(eth_nxp_enet_qos)
add_subdirectory(nxp_imx_netc)
add_subdirectory(dwc_xgmac)
```
- **功能**: 添加子目錄進行構建
- **說明**: 這些子目錄包含特定類型的驅動程式或功能模組

#### 3. `drivers/ethernet/phy/CMakeLists.txt`

**檔案內容**:
```cmake
# SPDX-License-Identifier: Apache-2.0

zephyr_library_sources_ifdef(CONFIG_PHY_GENERIC_MII	phy_mii.c)

# zephyr-keep-sorted-start
zephyr_library_sources_ifdef(CONFIG_PHY_ADIN2111	phy_adin2111.c)
zephyr_library_sources_ifdef(CONFIG_PHY_DM8806		phy_dm8806.c)
zephyr_library_sources_ifdef(CONFIG_PHY_MICROCHIP_KSZ8081	phy_microchip_ksz8081.c)
zephyr_library_sources_ifdef(CONFIG_PHY_MICROCHIP_T1S phy_microchip_t1s.c)
zephyr_library_sources_ifdef(CONFIG_PHY_MICROCHIP_VSC8541 phy_microchip_vsc8541.c)
zephyr_library_sources_ifdef(CONFIG_PHY_OA_TC14_PLCA_LIB phy_oa_tc14_plca.c)
zephyr_library_sources_ifdef(CONFIG_PHY_QUALCOMM_AR8031 phy_qualcomm_ar8031.c)
zephyr_library_sources_ifdef(CONFIG_PHY_REALTEK_RTL8211F phy_realtek_rtl8211f.c)
zephyr_library_sources_ifdef(CONFIG_PHY_TI_DP83825  phy_ti_dp83825.c)
zephyr_library_sources_ifdef(CONFIG_PHY_TI_DP83867  phy_ti_dp83867.c)
zephyr_library_sources_ifdef(CONFIG_PHY_TJA1103	        phy_tja1103.c)
# zephyr-keep-sorted-stop
```

**說明**:
- **功能**: 編譯 PHY（物理層）驅動程式
- **ENC28J60 相關**: ENC28J60 內建 PHY，不需要外部的 PHY 驅動程式
- **排序註釋**: `# zephyr-keep-sorted-start/stop` 是自動排序標記，幫助維護檔案列表順序

#### 4. `drivers/ethernet/dsa/CMakeLists.txt`

**檔案內容**:
```cmake
# SPDX-License-Identifier: Apache-2.0

zephyr_library_sources_ifdef(CONFIG_DSA_KSZ8XXX	dsa_ksz8xxx.c)
```

**說明**:
- **功能**: 編譯 DSA（Distributed Switch Architecture，分散式交換架構）驅動程式
- **ENC28J60 相關**: ENC28J60 不支援 DSA

#### 5. `drivers/ethernet/eth_nxp_enet_qos/CMakeLists.txt`

**檔案內容**:
```cmake
# Copyright 2024 NXP
# SPDX-License-Identifier: Apache-2.0

zephyr_library_sources_ifdef(CONFIG_ETH_NXP_ENET_QOS		eth_nxp_enet_qos.c)
zephyr_library_sources_ifdef(CONFIG_ETH_NXP_ENET_QOS_MAC	eth_nxp_enet_qos_mac.c)
```

**說明**:
- **功能**: 編譯 NXP ENET QoS 驅動程式
- **ENC28J60 相關**: 不相關，這是 NXP 特定的驅動程式

### 三、CMake 構建流程說明

#### 1. 構建流程

```
構建命令
  ↓
讀取 samples/net/dhcpv4_client/CMakeLists.txt
  ↓
載入 Zephyr 構建系統
  ↓
讀取 Kconfig 配置（prj.conf）
  ↓
解析設備樹（包含 overlay-enc28j60.overlay）
  ↓
檢測到 compatible = "microchip,enc28j60"
  ↓
自動設置 CONFIG_ETH_ENC28J60=y
  ↓
讀取 drivers/ethernet/CMakeLists.txt
  ↓
條件編譯 eth_enc28j60.c
  ↓
編譯應用程式源檔案（src/main.c）
  ↓
連結所有庫和驅動程式
  ↓
生成最終韌體
```

#### 2. CMake 關鍵函數說明

##### `zephyr_library()`
- **功能**: 創建一個 Zephyr 靜態庫目標
- **命名**: 根據目錄路徑自動命名（例如：`drivers__ethernet`）
- **用途**: 組織相關源檔案為一個庫

##### `zephyr_library_sources()`
- **功能**: 無條件添加源檔案到庫
- **語法**: `zephyr_library_sources(file1.c file2.c ...)`

##### `zephyr_library_sources_ifdef(CONFIG_XXX file.c)`
- **功能**: 根據 Kconfig 選項條件編譯源檔案
- **條件**: 僅當 `CONFIG_XXX` 為 `y` 時編譯
- **ENC28J60**: `CONFIG_ETH_ENC28J60` 控制 `eth_enc28j60.c` 的編譯

##### `zephyr_library_include_directories(path)`
- **功能**: 添加包含目錄
- **用途**: 讓源檔案可以 `#include` 該目錄下的標頭檔案

##### `zephyr_library_property(ALLOW_EMPTY TRUE)`
- **功能**: 允許庫為空（沒有源檔案）
- **用途**: 避免在沒有啟用任何驅動程式時報錯

##### `add_subdirectory(dir)`
- **功能**: 添加子目錄到構建系統
- **用途**: 遞歸處理子目錄的 CMakeLists.txt

### 四、ENC28J60 相關構建流程

#### 1. 驅動程式編譯流程

```
設備樹解析
  ↓
檢測 compatible = "microchip,enc28j60"
  ↓
設置 CONFIG_ETH_ENC28J60=y
  ↓
CMakeLists.txt 條件編譯
  ↓
編譯 eth_enc28j60.c
  ↓
連結到應用程式
```

#### 2. 編譯條件檢查

**自動啟用條件**:
- 設備樹中有 `compatible = "microchip,enc28j60"` 節點
- Kconfig 系統自動設置 `CONFIG_ETH_ENC28J60=y`
- CMake 檢測到此配置並編譯驅動程式

**依賴關係**:
- `CONFIG_ETH_ENC28J60` 會自動選擇 `CONFIG_SPI=y`
- 需要 `CONFIG_GPIO=y`（手動啟用或由其他驅動選擇）
- 需要 `CONFIG_NET_L2_ETHERNET=y`（由網路配置啟用）

### 五、完整專案構建結構

```
zephyr/
│
├── samples/net/dhcpv4_client/          # 應用程式專案
│   ├── CMakeLists.txt                   # 應用程式構建配置
│   │   └── 功能: 收集 src/*.c 並添加到 app 目標
│   │
│   ├── prj.conf                         # Kconfig 配置
│   │   └── 啟用: 網路、DHCPv4、ENC28J60 等
│   │
│   ├── overlay-enc28j60.overlay        # 設備樹覆蓋
│   │   └── 定義: ENC28J60 SPI 設備節點
│   │
│   └── src/main.c                       # 應用程式源檔案
│       └── 功能: DHCPv4 客戶端初始化和事件處理
│
└── drivers/ethernet/                    # 乙太網驅動程式庫
    ├── CMakeLists.txt                   # 驅動程式構建配置
    │   ├── 創建 zephyr_library()
    │   ├── 條件編譯: eth_enc28j60.c (CONFIG_ETH_ENC28J60)
    │   └── 添加子目錄: phy/, dsa/, etc.
    │
    ├── eth_enc28j60.c                   # ENC28J60 驅動程式實作
    │   └── 編譯條件: CONFIG_ETH_ENC28J60=y
    │
    ├── eth_enc28j60_priv.h              # 驅動程式私有標頭
    │   └── 內部使用：暫存器定義、結構體定義
    │
    ├── Kconfig                           # 乙太網驅動程式配置
    │   └── 包含: source "drivers/ethernet/Kconfig.enc28j60"
    │
    ├── Kconfig.enc28j60                  # ENC28J60 專用配置
    │   └── 定義: CONFIG_ETH_ENC28J60 及其子選項
    │
    ├── phy/                              # PHY 子目錄
    │   ├── CMakeLists.txt                # PHY 驅動構建配置
    │   └── phy_*.c                       # 各種 PHY 驅動程式
    │
    ├── dsa/                              # DSA 子目錄
    │   ├── CMakeLists.txt                # DSA 驅動構建配置
    │   └── dsa_*.c                       # DSA 驅動程式
    │
    └── eth_nxp_enet_qos/                 # NXP ENET QoS 子目錄
        ├── CMakeLists.txt                # NXP 驅動構建配置
        └── eth_nxp_enet_qos*.c           # NXP 驅動程式
```

### 六、構建命令與 CMake 變數

#### 1. 基本構建命令

```bash
# 使用 West 工具構建
west build -b <board> samples/net/dhcpv4_client \
  -- -DOVERLAY_CONFIG=overlay-enc28j60.overlay

# 使用 CMake 直接構建
cd samples/net/dhcpv4_client
mkdir build && cd build
cmake -GNinja -DBOARD=<board> \
  -DOVERLAY_CONFIG=../overlay-enc28j60.overlay ..
ninja
```

#### 2. 關鍵 CMake 變數

- **`ZEPHYR_BASE`**: Zephyr 根目錄路徑
- **`BOARD`**: 目標開發板名稱（例如：`nrf52840dk_nrf52840`）
- **`OVERLAY_CONFIG`**: 設備樹覆蓋檔案名稱
- **`CONFIG_ETH_ENC28J60`**: Kconfig 選項（自動設置）

#### 3. 構建輸出

構建成功後，會生成：
- `build/zephyr/zephyr.elf` - ELF 可執行檔案
- `build/zephyr/zephyr.hex` - Intel HEX 格式韌體
- `build/zephyr/zephyr.bin` - 二進制韌體檔案
- `build/zephyr/compile_commands.json` - 編譯命令數據庫（用於 IDE）

### 七、CMakeLists.txt 最佳實踐

#### 1. 條件編譯

**推薦做法**:
```cmake
# 使用 zephyr_library_sources_ifdef 進行條件編譯
zephyr_library_sources_ifdef(CONFIG_ETH_ENC28J60 eth_enc28j60.c)
```

**不推薦**:
```cmake
# 避免在 CMakeLists.txt 中使用 if() 進行簡單的條件編譯
if(CONFIG_ETH_ENC28J60)
  zephyr_library_sources(eth_enc28j60.c)
endif()
```

#### 2. 檔案組織

**推薦做法**:
```cmake
# 將相關驅動程式組織在同一 CMakeLists.txt 中
zephyr_library_sources_ifdef(CONFIG_ETH_ENC28J60 eth_enc28j60.c)
zephyr_library_sources_ifdef(CONFIG_ETH_DM9051 eth_dm9051.c)
```

**不推薦**:
```cmake
# 避免為每個驅動程式創建單獨的子目錄（除非必要）
```

#### 3. 依賴管理

**ENC28J60 依賴**:
- SPI 驅動程式（由 Kconfig 自動選擇）
- GPIO 驅動程式（手動啟用或由其他驅動選擇）
- 網路堆疊（由應用程式配置）

### 八、調試 CMake 構建問題

#### 1. 檢查驅動程式是否被編譯

```bash
# 構建時查看詳細輸出
west build -b <board> samples/net/dhcpv4_client -- -DOVERLAY_CONFIG=overlay-enc28j60.overlay -v

# 檢查編譯命令
grep -r "eth_enc28j60.c" build/
```

#### 2. 檢查 Kconfig 配置

```bash
# 查看實際配置
west build -b <board> samples/net/dhcpv4_client -- -DOVERLAY_CONFIG=overlay-enc28j60.overlay
cat build/zephyr/.config | grep ETH_ENC28J60
```

#### 3. 檢查設備樹

```bash
# 查看生成的設備樹
cat build/zephyr/zephyr.dts | grep -A 20 enc28j60
```

---

**最後更新**: 2024年

